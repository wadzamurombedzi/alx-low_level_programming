#include "main.h"

/**
 * more_numbers - prints 0 to 14 ten times
 * Return: Always 0
 */

void more_numbers(void)
{
	int x, y;

	for (x = 1; x < = 10; x++)
	{
		for (y = 1; y < = 14; y++)
		{
			for (y > = 10)
				_putchar('1');
			_putchar(j % 10 + '0');
		}
		putchar('\n');
	}
}

