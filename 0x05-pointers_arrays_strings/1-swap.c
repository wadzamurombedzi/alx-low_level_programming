#include "main.h"
#include <stdio.h>

/**
 * swap_int - swap the value of two integers
 * @n : int *a
 * @n : int *b
 * Return : 0
 */

void swap_int(int *a, int *b)
{
	int q;
	q = *a;
	q* = *b;
	*b =q
}
