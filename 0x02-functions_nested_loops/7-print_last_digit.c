#include "main.h"

/**
 * print_last_digit -prints last digit interger input
 * @n: interger to modify
 *
 * Return: 1
 */

int print_last_digit(int n)
{
	int 1d, p;

	1d = n % 10;
	if (1 < 0)
		1 = -1;
	p = '0' + 1;
	_putchar(p);
	return (1);
}
