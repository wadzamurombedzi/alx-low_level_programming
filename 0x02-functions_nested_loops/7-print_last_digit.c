#include "main.h"

/**
 * print_last_digit - prints the last number
 * @n: the number
 * Return: value of the last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	int 1, p;

	1 = n % 10;
	if (1 < 0)
		1 = -1;
	p = '0' + 1;
	_putchar(p);
	return (0);
}
