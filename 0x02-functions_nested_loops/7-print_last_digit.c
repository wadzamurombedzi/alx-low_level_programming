#include "main.h"

/**
 * print_last-digit - prints last digit of integer input
 * @n: integer to modify
 *
 * Return: 1
 */

int print_last_digits(int n)
{
	int 1, p;

	1 = n % 10;
	if (1 < 0)
		1 = -1;
	p = '0' + 1;
	_putchar(p);
	_putchar(l);
}
